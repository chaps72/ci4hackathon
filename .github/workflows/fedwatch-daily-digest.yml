name: FedWatch daily digest

on:
  schedule:
    # Twin crons so one always lands at 3pm America/New_York across DST;
    # the script skips the off-hour firing, weekends, and US federal holidays.
    - cron: "0 19 * * 1-5"
    - cron: "0 20 * * 1-5"
  workflow_dispatch: {}

jobs:
  digest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests anthropic

      - name: Send daily digest
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          FEDWATCH_APP_URL: ${{ secrets.FEDWATCH_APP_URL }}
          DIGEST_DAYS_BACK: "2"
          FEDWATCH_FOCUS: nih
        run: python send_digest.py
