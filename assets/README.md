# Assets

Drop the Emory Research logo here as `emory_research_logo.svg` (preferred) or
`.png` / `.jpg`. The app header picks it up automatically; if no file is
present it falls back to a navy "Emory Research" text wordmark.

Recommended: a horizontal logo around 34px tall. Use a version with dark/navy
marks (the header background is white).
