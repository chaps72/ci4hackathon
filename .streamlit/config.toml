[theme]
primaryColor = "#012169"              # Emory navy (tabs, sliders, focus)
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f5f5f7"  # light gray (sidebar / inputs)
textColor = "#1d1d1f"                 # near-black
font = "sans serif"
