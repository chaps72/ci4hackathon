[theme]
primaryColor = "#2457d6"              # accent (buttons, tabs, sliders, focus)
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f7f8fa"  # sidebar / inputs / panels
textColor = "#11181c"
font = "sans serif"
