[theme]
primaryColor = "#012169"              # Emory blue (tabs, sliders, focus states)
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#eef1f7"  # light Emory blue tint (sidebar, inputs)
textColor = "#26282a"
